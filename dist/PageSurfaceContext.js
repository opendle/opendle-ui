import { createContext } from "react";
/** The nearest page owns the graph composition mode. */
export const PageSurfaceEdgeContext = createContext(false);
//# sourceMappingURL=PageSurfaceContext.js.map